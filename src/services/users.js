// @flow
// import type { Users } from "../redux/slices/users/types";

export const LOCALSTORAGEREF: string = "USERS";

// export const createUser = () => {
//   const users = localStorage.getItem(LOCALSTORAGEREF);

//   if (users && users.length > 0) {
//     return [];
//   }
// };
